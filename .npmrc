# Enable the hoisting mechanism of pnpm.
shamefully-hoist=true

# Disable the package-lock.json or yarn.lock lock files automatically generated by npm and yarn to avoid conflicts between these files and pnpm's dependency management mechanism.
# package-lock=false

# Enable the hoisting mechanism.
shamefully-hoist=true

# Use preinstall, install, and postinstall scripts for all packages to ensure reliable and secure dependency installations.
ignore-scripts=true
